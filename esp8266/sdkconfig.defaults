# UART0 for KISS — no console on this port
CONFIG_ESP_CONSOLE_UART_NONE=y

# WiFi buffers
CONFIG_ESP8266_WIFI_RX_BUFFER_NUM=30

# FreeRTOS tick rate
CONFIG_FREERTOS_HZ=1000

# Flash size (4MB)
CONFIG_ESPTOOLPY_FLASHSIZE_4MB=y

# Partition table
CONFIG_PARTITION_TABLE_SINGLE_APP=y
